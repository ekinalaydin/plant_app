import 'package:flutter/material.dart';
import 'package:plant_app/widgets/card_list.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: CardList());
  }
}
